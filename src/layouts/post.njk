---
layout: base
---

<main id="main">
    <article class="prose prose-invert">
        {% if title %}
            <h3>{{ title | safe }}</h3>
        {% endif %}
        {{ content | safe }}
    </article>
</main>
